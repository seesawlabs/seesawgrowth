---
/* The AI Reality Check as a standalone offer is retired — see
   docs/10-package-plan.md. It sold a one-hour call producing a written
   assessment in three business days; the package inverts that, with the
   analysis arriving before a 45-minute session. Keeping the page would leave a
   surface describing an offer we no longer run, which is exactly the failure
   this refactor is fixing.

   The design record survives in docs/05-reality-check-spec.md, and the session
   still runs on the interview guide in docs/07. */
export const prerender = false;
return Astro.redirect('/#session', 302);
---
