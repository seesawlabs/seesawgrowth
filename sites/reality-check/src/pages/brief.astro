---
/* /brief now lives at the site root — see src/pages/index.astro. Kept as a
   redirect because the URL is in a Slack thread, in the sample brief's banner,
   and in whatever campaign link outlives this refactor. 301: this one really is
   permanent. */
export const prerender = false;
return Astro.redirect('/', 301);
---
