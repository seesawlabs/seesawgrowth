---
/* This walked through the structure of the old post-call assessment. The
   deliverable is now the analysis, and we have a real one to show instead of a
   description of one — /sample-brief is the brief we ran on ourselves,
   unedited. A structural walkthrough of a retired document is worse than
   nothing: it tells a visitor to expect something they will not receive. */
export const prerender = false;
return Astro.redirect('/sample-brief', 301);
---
